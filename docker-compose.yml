services:
  db:
    image: postgres:16-alpine
    ports:
      - "5026:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: jackpot_development
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
